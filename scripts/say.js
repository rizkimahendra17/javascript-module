//ditambah export agar bisa di akses keluar, karna kita pakai module

export function sayHello(name) {
    console.info(`Hello ${name}`);
}

export function sayGoodBye(name) {
        console.info(`Good By ${name}`);
}

